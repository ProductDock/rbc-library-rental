spring:
  kafka:
    enabled: true
    bootstrap-servers: ${kafka.server.url:localhost:9092}
    producer:
      client-id: kafka-message-producer
      acks: all
      value-seriliazer: org.apache.kafka.common.serialization.StringSerializer
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      enable-auto-commit: true
      auto-offset-reset: latest
      group-id: ${consumer.group:group}
    topic:
      book-status: ${kafka.topic:book-status}

  data:
    mongodb:
      authentication-database: ${mongo.database.name:rental-db}
      host: ${mongo.server.url:localhost}
      port: ${mongo.server.port:29017}
      username: ${mongo.username:root1}
      password: ${mongo.password:root1}
      database: ${mongo.database.name:rental-db}

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: accounts.google.com
          jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs

server:
  error:
    include-message: always